<h2>Here are our Restaurants</h2>
<ul>
  <% @restaurants.each do |restaurant| %>
    <li><%= link_to "#{restaurant.id} | #{restaurant.name}", restaurant_path(restaurant) %></li>
  <% end %>
</ul>
<%= link_to "Add a restaurant sweetie", new_restaurant_path %>
